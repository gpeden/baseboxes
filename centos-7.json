{
  "builders": [

    {
      "boot_command": [
        "<tab> text ks=http://{{ .HTTPIP }}:{{ .HTTPPort }}/{{user `ks_path`}}<enter><wait>"
      ],
      "boot_wait": "10s",
      "disk_size": 40960,
      "guest_os_type": "linux",
      "headless": "{{ user `headless` }}",
      "http_directory": "http",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_checksum_type": "{{user `iso_checksum_type`}}",
      "iso_url": "{{user `mirror`}}/{{user `mirror_directory`}}/{{user `iso_name`}}",
      "output_directory": "{{user `output_directory`}}",
      "shutdown_command": "echo 'vagrant' | sudo -S /sbin/halt -h -p",
      "ssh_pty" : true,
      "ssh_password": "vagrant",
      "ssh_port": 22,
      "ssh_username": "vagrant",
      "ssh_wait_timeout": "10000s",
      "type": "vmware-iso",
      "vm_name": "{{ user `template` }}",
      "vmx_data": {
        "cpuid.coresPerSocket": "1",
        "ethernet0.virtualDev": "vmxnet3",
        "memsize": "2048",
        "numvcpus": "4"
      }
    }
  ],
  "post-processors": [
    [
      {
      "output": "builds/{{user `box_basename`}}.{{.Provider}}.vmx",
      "type": "vagrant",
      "keep_input_artifact": true
      },
      {
      "type": "shell-local",
      "script": "scripts/common/vmx-to-ova.sh",
      "only": ["vmware-iso"],
      "environment_vars": ["PACKER_OUTPUT_DIRECTORY={{user `output_directory`}}"]
      }
    ]
  ],
  "provisioners": [
    {
      "destination": "/tmp/bento-metadata.json",
      "source": "{{user `metadata`}}",
      "type": "file"
    },
    {
      "environment_vars": [
        "HOME_DIR=/home/vagrant",
        "http_proxy={{user `http_proxy`}}",
        "https_proxy={{user `https_proxy`}}",
        "no_proxy={{user `no_proxy`}}"
      ],
      "execute_command": "echo 'vagrant' | {{.Vars}} sudo -S -E sh -eux '{{.Path}}'",
      "execute_command": "sudo yum install -y open-vm-tools",
      "scripts": [
        "scripts/common/sshd.sh",
        "scripts/centos/networking.sh",
        "scripts/common/vagrant.sh",
        "scripts/centos/7/cleanup.sh",
        "scripts/common/minimize.sh"
      ],
      "type": "shell"
    }
  ],
  "variables": {
    "box_basename": "centos-7",
    "build_timestamp": "{{isotime \"20060102150405\"}}",
    "git_revision": "__unknown_git_revision__",
    "headless": "true",
    "http_proxy": "{{env `http_proxy`}}",
    "https_proxy": "{{env `https_proxy`}}",
    "iso_checksum": "907e5755f824c5848b9c8efbb484f3cd945e93faa024bad6ba875226f9683b16",
    "iso_checksum_type": "none",
    "iso_name": "CentOS-7-x86_64-DVD-1511.iso",
    "ks_path": "centos-7/ks.cfg",
    "metadata": "floppy/dummy_metadata.json",
    "mirror": "http://mirrors.kernel.org/centos",
    "mirror_directory": "7.2.1511/isos/x86_64",
    "name": "centos-7",
    "no_proxy": "{{env `no_proxy`}}",
    "template": "centos-7",
    "version": "2.1.TIMESTAMP",
    "output_directory": "packer-centos-7-vmware"

  }
}
